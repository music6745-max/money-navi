name: Codex P112 Money Run

on:
  push:
    branches: [master]

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.message, 'trigger P112 money runner now')
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0
      - name: Apply staged P112 patch and clean up
        shell: bash
        run: |
          set -euo pipefail
          awk 'BEGIN{flag=0} /^H4sIA/{flag=1} /^PATCH_B64$/{flag=0} flag{print}' .github/workflows/codex-p112-money.yml > /tmp/codex-p112-money.patch.gz.b64
          test -s /tmp/codex-p112-money.patch.gz.b64
          base64 -d /tmp/codex-p112-money.patch.gz.b64 | gunzip > /tmp/codex-p112-money.patch
          git apply --binary /tmp/codex-p112-money.patch
          rm -f .github/workflows/codex-p112-money.yml .github/workflows/codex-p112-money-run.yml .github/codex-p112-money-trigger.txt
          git config user.name "codex-automation"
          git config user.email "codex-automation@users.noreply.github.com"
          git add -A src/app/guide/parent-favorite-music-songs-record/page.tsx src/app/kaigo/layout.tsx src/app/sitemap.ts .github/workflows/codex-p112-money.yml .github/workflows/codex-p112-money-run.yml .github/codex-p112-money-trigger.txt
          git diff --cached --check
          git commit -m "feat: add P112 favorite music guide"
          git push origin HEAD:master
