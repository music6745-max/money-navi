name: Apply P85 Money Patch Trigger

on:
  push:
    paths:
      - ".github/workflows/apply-p85-money.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply P85 money patch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git show HEAD~1:.github/workflows/apply-p85-money.yml \
            | sed -n "/base64 -d > \/tmp\/p85.patch <<'PATCH_B64'/,/^PATCH_B64$/p" \
            | sed '1d;$d' > /tmp/p85.patch.b64
          base64 -d /tmp/p85.patch.b64 > /tmp/p85.patch
          git apply /tmp/p85.patch
          rm -f .github/workflows/apply-p85-money.yml
          git add src/app/guide/parent-home-family-stationery-writing-items-record/page.tsx src/app/kaigo/layout.tsx src/app/sitemap.ts .github/workflows/apply-p85-money.yml
          git commit -m "feat: add P85 stationery guide"
          git push
